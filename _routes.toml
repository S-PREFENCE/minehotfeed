# Cloudflare Pages 构建配置
# 前端构建命令
[build]
  command = "cd src/frontend && npm install && npm run build"
  output = "src/frontend/dist"

# 环境变量
[env]
  VITE_API_BASE = "https://hotfeed-backend.onrender.com/api"
